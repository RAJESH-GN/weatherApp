import { MeasurementTypePipe } from './measurement-type.pipe';

describe('MeasurementTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MeasurementTypePipe();
    expect(pipe).toBeTruthy();
  });
});
