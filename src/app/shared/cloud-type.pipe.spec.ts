import { CloudTypePipe } from './cloud-type.pipe';

describe('CloudTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CloudTypePipe();
    expect(pipe).toBeTruthy();
  });
});
